{
  "//": "TEMPLATE — copy to appsettings.Local.json, which is git-ignored. Documents STRUCTURE ONLY. Never put a real secret here; this file is committed (coding-standards CF-3, CF-6). Local overrides take precedence over appsettings.Development.json.",

  "Application": {
    "PublicBaseUrl": ""
  },
  "Api": {
    "DefaultPageSize": 50,
    "MaxPageSize": 200
  },
  "Cors": {
    "AllowedOrigins": [ "" ],
    "AllowCredentials": true
  },
  "//Jwt": "The signing key is C4 material and must never be committed (security-architecture §17). Generate a development keypair locally: openssl genrsa -out jwt-dev.pem 2048 -- then paste the PEM here, in appsettings.Local.json, which is git-ignored. Rotation adds the retired key's PUBLIC half to PreviousKeys so tokens it signed stay valid until they expire.",
  "Jwt": {
    "Issuer": "https://localhost:5001",
    "Audience": "maintorbit-api",
    "SigningKey": {
      "KeyId": "local-1",
      "PrivateKeyPem": "-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"
    }
  },
  "//Encryption": "C4 key material, never committed. Generate a development key locally: openssl rand -base64 32 -- then paste it into appsettings.Local.json, which is git-ignored. Losing it makes every stored MFA secret permanently unreadable, so a local key is disposable but a deployment key is not (10-key-management §5).",
  "Encryption": {
    "DataKey": "",
    "DataKeyVersion": 1
  },
  "Persistence": {
    "ConnectionString": "Host=localhost;Port=5432;Database=maintorbit_dev;Username=maintorbit;Password="
  },
  "HealthChecks": {
    "Enabled": true
  },
  "Logging": {
    "LogLevel": {
      "Default": "Debug"
    }
  }
}
